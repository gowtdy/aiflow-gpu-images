services:
  htmlframes-server:
    image: htmlframes-image:0.1
    hostname: htmlframes-server
    container_name: htmlframes-server
    restart: unless-stopped
    network_mode: host
    dns:
      - 8.8.8.8
      - 1.1.1.1
      - 192.168.3.1
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    environment:
      - AIGC_UID=${AIGC_UID:-1001}
      - AIGC_GID=${AIGC_GID:-1001}
      - TZ=Asia/Shanghai
    env_file:
      - /home/aigc/data/env/.env_dskey
    extra_hosts:
      - "host.docker.internal:host-gateway"
    #tty: true
